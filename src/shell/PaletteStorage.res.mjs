// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Idb from "./Idb.res.mjs";
import * as Palette from "../core/Palette.res.mjs";

let storageKey = "palette";

async function load() {
  let stored = await Idb.get(storageKey);
  if (stored == null) {
    return;
  } else {
    return Palette.parse(stored);
  }
}

function save(palette) {
  Idb.set(storageKey, Palette.toTag(palette));
}

export {
  load,
  save,
}
/* Idb Not a pure module */
