// Generated by ReScript, PLEASE EDIT WITH CARE


import DOMPurify from "dompurify";

// KaTeX emits MathML and styled spans; Mermaid emits SVG carrying its own <style>.
// Both are allowed, but the dangerous surface is closed explicitly instead of being
// left to whatever the profile defaults happen to be.
const SANITIZE_CONFIG = {
  USE_PROFILES: { html: true, svg: true, svgFilters: true, mathMl: true },
  // Every Spectrum tag the renderer can emit, generated from the same registry.
  // These are the app's own components, not scriptable content: the dangerous tags
  // stay forbidden below regardless.
  ADD_TAGS: ["style", "overlay-trigger", "sp-accordion", "sp-accordion-item", "sp-action-bar", "sp-action-button", "sp-action-group", "sp-action-menu", "sp-alert-banner", "sp-alert-dialog", "sp-asset", "sp-avatar", "sp-badge", "sp-breadcrumb-item", "sp-breadcrumbs", "sp-button", "sp-button-group", "sp-card", "sp-checkbox", "sp-clear-button", "sp-close-button", "sp-coach-indicator", "sp-coachmark", "sp-color-area", "sp-color-field", "sp-color-handle", "sp-color-loupe", "sp-color-slider", "sp-color-wheel", "sp-combobox", "sp-contextual-help", "sp-dialog", "sp-dialog-base", "sp-dialog-wrapper", "sp-divider", "sp-dropzone", "sp-field-group", "sp-field-label", "sp-grid", "sp-help-text", "sp-icon", "sp-illustrated-message", "sp-infield-button", "sp-link", "sp-menu", "sp-menu-divider", "sp-menu-group", "sp-menu-item", "sp-meter", "sp-number-field", "sp-overlay", "sp-picker", "sp-picker-button", "sp-popover", "sp-progress-bar", "sp-progress-circle", "sp-radio", "sp-radio-group", "sp-search", "sp-sidenav", "sp-sidenav-heading", "sp-sidenav-item", "sp-slider", "sp-slider-handle", "sp-split-view", "sp-status-light", "sp-swatch", "sp-swatch-group", "sp-switch", "sp-tab", "sp-tab-panel", "sp-table", "sp-table-body", "sp-table-cell", "sp-table-checkbox-cell", "sp-table-head", "sp-table-head-cell", "sp-table-row", "sp-tabs", "sp-tabs-overflow", "sp-tag", "sp-tags", "sp-textfield", "sp-theme", "sp-thumbnail", "sp-toast", "sp-tooltip", "sp-tooltip-openable", "sp-top-nav", "sp-top-nav-item", "sp-tray", "sp-truncated", "sp-underlay"],
  // The union of their attributes. A component silently losing an attribute to the
  // sanitiser is the kind of failure that looks like the component is broken.
  ADD_DATA_URI_TAGS: [],
  // name and open carry the accordion's grouping and state; id is the directive's
  // own identifier. Without these the sanitiser strips exactly the attributes the
  // directives exist to set.
  ADD_ATTR: [
    "class", "style", "target", "rel", "id", "name", "open", "hidden",
    // The range control's own shape. formaction and friends stay forbidden below.
    "type", "min", "max", "step", "value", "for",
    "action", "active", "allow-multiple", "allow-outside-click", "allowed-keys", "asset", "auto", "autocomplete", "autofocus", "background", "block", "border", "cancel-label", "checked", "collapsible", "color", "compact", "confirm-label", "cover", "current-step", "default-value", "delayed", "deletable", "density", "description", "direction", "disabled", "dismiss-label", "dismissable", "dismissible", "dragged", "dragging", "dropEffect", "editable", "emphasized", "enableTabsScroll", "error", "expanded", "fill-start", "filled", "fixed", "flexible", "focusableSelector", "focused", "footer", "for", "force-popover", "format-options", "gap", "grows", "has-asset", "has-keyboard-dismiss", "has-submenu", "head-cell", "heading", "headline", "headline-visibility", "hero", "hero-label", "hide-stepper", "highlight", "hold-affordance", "holdValueOnEscape", "horizontal", "hue", "icon", "icon-label", "icons", "id", "ignore", "ignore-url-parts", "indeterminate", "inline", "invalid", "is-decorative", "isLastOfType", "item", "itemLabel", "itemSize", "itemValue", "items", "justified", "keyboard-focused", "label", "label-next", "label-previous", "label-visibility", "label-x", "label-y", "lang", "layer", "level", "manage-tab-index", "max", "max-visible-items", "maxlength", "media-type", "menu-label", "method", "min", "minlength", "mixed-value", "mobile-back-label", "mobile-view", "mode", "modifierKeys", "multiline", "name", "no-divider", "no-wrap", "nothing", "offset", "open", "options", "over-background", "padding", "pattern", "pending", "pending-label", "placeholder", "placement", "position", "primary-cta", "primary-max", "primary-min", "primary-size", "primarySize", "progress", "quiet", "readonly", "receives-focus", "required", "resizable", "responsive", "role", "rounding", "rows", "scale", "scroller", "secondary-cta", "secondary-label", "secondary-max", "secondary-min", "select-all-label", "selectable", "selected", "selectionIndicatorStyle", "selects", "selects-single", "self-managed", "shape", "shortcut-key", "side-aligned", "side-label", "size", "sort-direction", "sort-key", "sortable", "splitter-pos", "src", "static-color", "step", "step-modifier", "subheading", "success-message", "system", "tabindex", "tick-labels", "tick-step", "timeout", "tip", "tip-padding", "tipPadding", "toggles", "tooltip-placement", "total-steps", "treatment", "trigger", "triggered-by", "type", "underlay", "valid", "value", "value-separator", "variant", "vertical", "view-color", "viewSize", "x", "y",
  ],
  FORBID_TAGS: ["script", "iframe", "object", "embed", "form", "base", "meta", "link"],
  FORBID_ATTR: ["srcdoc", "formaction", "ping", "http-equiv"],
  SANITIZE_DOM: true,
  KEEP_CONTENT: false,
  // DOMPurify parses the fragment as a document and gives back the BODY alone, so
  // anything the HTML parser hoists into <head> is silently lost — and a leading
  // <style> is hoisted, while the very same <style> written after any element is
  // not. That position dependence is a parser accident, not a decision here: style
  // is in ADD_TAGS deliberately, for the one Mermaid ships inside its SVG.
  //
  // It cost the explore view its whole grid. regular-table builds its shadow root
  // from a template that OPENS with two <style> blocks and then reads the children
  // back BY POSITION — let [, sub_cell_style, virtual_panel, table_clip] = children
  // — so losing the two left it four children short of two, every field bound to the
  // wrong node, and _table_clip undefined. The crash lands far away and much later,
  // inside Perspective's own draw, as "Cannot read properties of undefined (reading
  // clientWidth)".
  //
  // FORCE_BODY is DOMPurify's own remedy: it prepends a throwaway element so the
  // parser stays in body from the first token. Nothing is exempted from sanitising —
  // only kept where it was written.
  FORCE_BODY: true,
};
;

let toTrustedHtml = (function (html) {
  // RETURN_TRUSTED_TYPE makes DOMPurify hand back a TrustedHTML produced by its own
  // "dompurify" policy, which the CSP names explicitly. Wrapping this in a second
  // policy of ours was worse than redundant: DOMPurify assigns the *unsanitised*
  // payload to innerHTML internally while parsing, so when its own policy is missing
  // that assignment throws, sanitize() blows up, and the preview silently renders
  // nothing at all.
  return DOMPurify.sanitize(html, { ...SANITIZE_CONFIG, RETURN_TRUSTED_TYPE: true });
});

let sanitizeToString = (function (html) {
  return DOMPurify.sanitize(html, SANITIZE_CONFIG);
});

function setInnerHtml(prim0, prim1) {
  prim0.innerHTML = prim1;
}

export {
  toTrustedHtml,
  setInnerHtml,
  sanitizeToString,
}
/*  Not a pure module */
