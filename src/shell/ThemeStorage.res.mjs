// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Idb from "./Idb.res.mjs";
import * as Theme from "../core/Theme.res.mjs";

let storageKey = "theme";

function systemPreference() {
  return Theme.fromPrefersDark(window.matchMedia("(prefers-color-scheme: dark)").matches);
}

async function load() {
  let stored = await Idb.get(storageKey);
  if (stored == null) {
    return;
  } else {
    return Theme.parse(stored);
  }
}

function save(theme) {
  Idb.set(storageKey, Theme.toTag(theme));
}

export {
  systemPreference,
  load,
  save,
}
/* Idb Not a pure module */
