// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Idb from "./Idb.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let decode = (function (text) {
  try {
    const parsed = JSON.parse(text);
    return Array.isArray(parsed) ? parsed.filter((p) => typeof p === "string") : [];
  } catch {
    return [];
  }
});

async function list(app) {
  let stored = await Idb.get("derived:" + app);
  if (stored == null) {
    if (stored === null) {
      return [];
    } else {
      return [];
    }
  } else {
    return decode(stored);
  }
}

async function mark(app, path) {
  let held = await list(app);
  if (!held.includes(path)) {
    held.push(path);
    return await Idb.set("derived:" + app, Stdlib_Option.getOr(JSON.stringify(held), "[]"));
  }
}

async function rename(from, to_) {
  let stored = await Idb.get("derived:" + from);
  if (stored == null) {
    return;
  }
  await Idb.set("derived:" + to_, stored);
  return await Idb.remove("derived:" + from);
}

export {
  list,
  mark,
  rename,
}
/* Idb Not a pure module */
