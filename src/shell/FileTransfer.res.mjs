// Generated by ReScript, PLEASE EDIT WITH CARE

import * as AppFile from "../core/AppFile.res.mjs";

function mimeFor(filename) {
  let lower = filename.toLowerCase();
  if (lower.endsWith(".json")) {
    return "application/json;charset=utf-8";
  } else if (lower.endsWith(".md") || lower.endsWith(".markdown")) {
    return "text/markdown;charset=utf-8";
  } else {
    return "text/plain;charset=utf-8";
  }
}

let write = (function (filename, text, mime) {
  const blob = new Blob([text], { type: mime });
  const url = URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = url;
  anchor.download = filename;
  // Appended before the click: Firefox ignores a click on an anchor that is not in
  // the document, and the failure is silent.
  document.body.appendChild(anchor);
  anchor.click();
  anchor.remove();
  // Deferred, because revoking it in the same task can cancel the download that the
  // click has only just started.
  setTimeout(function () { URL.revokeObjectURL(url); }, 0);
});

function download(filename, text) {
  write(filename, text, mimeFor(filename));
}

let open_ = (function (accept) {
  return new Promise(function (resolve) {
    const input = document.createElement("input");
    input.type = "file";
    input.accept = accept;
    input.style.display = "none";
    document.body.appendChild(input);
    const done = function (value) {
      input.remove();
      resolve(value);
    };
    input.addEventListener("change", function () {
      const file = input.files && input.files[0];
      if (!file) return done(undefined);
      file.text()
        .then(function (text) { done([file.name, text]); })
        // An unreadable file is "no file", the same as a dismissed picker: there is
        // nothing the caller could do differently, and nothing to report but that
        // no document arrived.
        .catch(function () { done(undefined); });
    });
    input.addEventListener("cancel", function () { done(undefined); });
    input.click();
  });
});

function readTextFile(acceptOpt, receive) {
  let accept = acceptOpt !== undefined ? acceptOpt : ".json,application/json";
  open_(accept).then(chosen => {
    if (chosen !== undefined) {
      return receive(chosen[1]);
    }
  });
}

function readDocument() {
  return open_(AppFile.accept);
}

let writeBinary = (function (filename, bytes, mime) {
  const blob = new Blob([bytes], { type: mime });
  const url = URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = url;
  anchor.download = filename;
  document.body.appendChild(anchor);
  anchor.click();
  anchor.remove();
  setTimeout(function () { URL.revokeObjectURL(url); }, 0);
});

function downloadBinary(filename, bytes) {
  writeBinary(filename, bytes, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
}

let openBinary = (function (accept) {
  return new Promise(function (resolve) {
    const input = document.createElement("input");
    input.type = "file";
    input.accept = accept;
    input.style.display = "none";
    document.body.appendChild(input);
    const done = function (value) {
      input.remove();
      resolve(value);
    };
    input.addEventListener("change", function () {
      const file = input.files && input.files[0];
      if (!file) return done(undefined);
      file.arrayBuffer()
        .then(function (buffer) { done(buffer); })
        .catch(function () { done(undefined); });
    });
    input.addEventListener("cancel", function () { done(undefined); });
    input.click();
  });
});

function readBinaryFile(accept, receive) {
  openBinary(accept).then(chosen => {
    if (chosen !== undefined) {
      return receive(chosen);
    }
  });
}

let readAttachment = (function (maxBytes) {
  return new Promise(function (resolve) {
    const input = document.createElement("input");
    input.type = "file";
    input.style.display = "none";
    document.body.appendChild(input);
    const done = function (value) {
      input.remove();
      resolve(value);
    };
    input.addEventListener("change", function () {
      const file = input.files && input.files[0];
      if (!file) return done(undefined);
      if (file.size > maxBytes)
        return done({ name: file.name, dataUrl: "", tooBig: true });
      const reader = new FileReader();
      reader.onload = function () {
        done({ name: file.name, dataUrl: String(reader.result), tooBig: false });
      };
      reader.onerror = function () { done(undefined); };
      reader.readAsDataURL(file);
    });
    input.addEventListener("cancel", function () { done(undefined); });
    input.click();
  });
});

export {
  download,
  readTextFile,
  readDocument,
  downloadBinary,
  readBinaryFile,
  readAttachment,
}
/* No side effect */
