// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Route from "../core/Route.res.mjs";

let fragment = (function () { return location.hash; });

let changed = "rn:navigate";

let push = (function (value) {
  history.pushState(null, "", value);
  window.dispatchEvent(new Event("rn:navigate"));
});

let replaceState = (function (value) {
  history.replaceState(null, "", value);
  window.dispatchEvent(new Event("rn:navigate"));
});

function current() {
  return Route.parse(location.pathname);
}

function navigate(route) {
  let next = Route.toPath(route);
  if (location.pathname === next) {
    return;
  } else {
    return push(next);
  }
}

function replace(route) {
  replaceState(Route.toPath(route));
}

function subscribe(onChange) {
  let handler = () => onChange(Route.parse(location.pathname));
  window.addEventListener("popstate", handler);
  window.addEventListener(changed, handler);
  return () => {
    window.removeEventListener("popstate", handler);
    window.removeEventListener(changed, handler);
  };
}

function follow(route) {
  return event => {
    if (!event.defaultPrevented && event.button === 0 && !event.metaKey && !event.ctrlKey && !event.shiftKey && !event.altKey) {
      event.preventDefault();
      return navigate(route);
    }
  };
}

export {
  fragment,
  current,
  navigate,
  replace,
  subscribe,
  follow,
}
/* Route Not a pure module */
