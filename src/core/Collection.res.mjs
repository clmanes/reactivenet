// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let empty = {
  records: []
};

function field(record, name) {
  let needle = name.toLowerCase();
  return Stdlib_Option.map(record.fields.find(field => field.name.toLowerCase() === needle), field => field.value);
}

function find(collection, id) {
  return collection.records.find(record => record.id === id);
}

function insert(collection, record) {
  return {
    records: collection.records.concat([record])
  };
}

function remove(collection, id) {
  return {
    records: collection.records.filter(record => record.id !== id)
  };
}

function update(collection, id, changes) {
  return {
    records: collection.records.map(record => {
      if (record.id !== id) {
        return record;
      } else {
        return {
          id: record.id,
          fields: Stdlib_Array.reduce(changes, record.fields, (fields, change) => {
            if (fields.some(field => field.name.toLowerCase() === change.name.toLowerCase())) {
              return fields.map(field => {
                if (field.name.toLowerCase() === change.name.toLowerCase()) {
                  return {
                    name: field.name,
                    value: change.value
                  };
                } else {
                  return field;
                }
              });
            } else {
              return fields.concat([change]);
            }
          })
        };
      }
    })
  };
}

function size(collection) {
  return collection.records.length;
}

function isEmpty(collection) {
  return collection.records.length === 0;
}

export {
  empty,
  field,
  find,
  insert,
  remove,
  update,
  size,
  isEmpty,
}
/* No side effect */
