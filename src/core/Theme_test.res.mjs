// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Theme from "./Theme.res.mjs";
import * as Buntest from "bun:test";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

Buntest.describe("Theme", () => {
  Buntest.test("toggles between the two polarities", () => {
    Buntest.expect(Theme.toTag(Theme.toggle("Light"))).toBe("dark");
    Buntest.expect(Theme.toTag(Theme.toggle("Dark"))).toBe("light");
  });
  Buntest.test("toggling twice is the identity", () => {
    Buntest.expect(Theme.toggle(Theme.toggle("Light"))).toEqual("Light");
    Buntest.expect(Theme.toggle(Theme.toggle("Dark"))).toEqual("Dark");
  });
  Buntest.test("reports polarity", () => {
    Buntest.expect(Theme.isDark("Dark")).toBe(true);
    Buntest.expect(Theme.isDark("Light")).toBe(false);
  });
  Buntest.test("maps the OS preference", () => {
    Buntest.expect(Theme.isDark(Theme.fromPrefersDark(true))).toBe(true);
    Buntest.expect(Theme.isDark(Theme.fromPrefersDark(false))).toBe(false);
  });
  Buntest.test("round-trips through its tag", () => {
    Buntest.expect(Theme.parse(Theme.toTag("Light"))).toEqual("Light");
    Buntest.expect(Theme.parse(Theme.toTag("Dark"))).toEqual("Dark");
  });
  Buntest.test("rejects unknown tags", () => {
    Buntest.expect(Stdlib_Option.isNone(Theme.parse("darkest"))).toBe(true);
  });
});

/*  Not a pure module */
