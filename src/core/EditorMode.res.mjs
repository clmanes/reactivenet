// Generated by ReScript, PLEASE EDIT WITH CARE


function showEditor(state, visible) {
  return {
    editing: state.editing,
    editorVisible: visible,
    previewWidth: visible ? state.previewWidth : "Full",
    frontmatterVisible: state.frontmatterVisible,
    directivesVisible: state.directivesVisible,
    dataVisible: state.dataVisible
  };
}

function useEditing(state, editing) {
  return {
    editing: editing,
    editorVisible: true,
    previewWidth: state.previewWidth,
    frontmatterVisible: state.frontmatterVisible,
    directivesVisible: state.directivesVisible,
    dataVisible: state.dataVisible
  };
}

function togglePreviewWidth(state) {
  let match = state.previewWidth;
  let tmp;
  tmp = match === "Full" ? "Phone" : "Full";
  return {
    editing: state.editing,
    editorVisible: state.editorVisible,
    previewWidth: tmp,
    frontmatterVisible: state.frontmatterVisible,
    directivesVisible: state.directivesVisible,
    dataVisible: state.dataVisible
  };
}

function previewIsPhone(state) {
  return state.previewWidth === "Phone";
}

function toggleFrontmatter(state) {
  return {
    editing: state.editing,
    editorVisible: state.editorVisible,
    previewWidth: state.previewWidth,
    frontmatterVisible: !state.frontmatterVisible,
    directivesVisible: state.directivesVisible,
    dataVisible: state.dataVisible
  };
}

function toggleDirectives(state) {
  return {
    editing: state.editing,
    editorVisible: state.editorVisible,
    previewWidth: state.previewWidth,
    frontmatterVisible: state.frontmatterVisible,
    directivesVisible: !state.directivesVisible,
    dataVisible: state.dataVisible
  };
}

function toggleData(state) {
  return {
    editing: state.editing,
    editorVisible: state.editorVisible,
    previewWidth: state.previewWidth,
    frontmatterVisible: state.frontmatterVisible,
    directivesVisible: state.directivesVisible,
    dataVisible: !state.dataVisible
  };
}

function showsMarkdownEditor(state) {
  if (state.editorVisible) {
    return state.editing === "Markdown";
  } else {
    return false;
  }
}

function showsBlockEditor(state) {
  if (state.editorVisible) {
    return state.editing === "Blocks";
  } else {
    return false;
  }
}

function showsFrontmatterForm(state) {
  if (showsBlockEditor(state)) {
    return state.frontmatterVisible;
  } else {
    return false;
  }
}

function showsDirectiveFields(state) {
  if (showsBlockEditor(state)) {
    return state.directivesVisible;
  } else {
    return false;
  }
}

function previewIsFullWidth(state) {
  return !state.editorVisible;
}

let initial = {
  editing: "Markdown",
  editorVisible: true,
  previewWidth: "Full",
  frontmatterVisible: true,
  directivesVisible: false,
  dataVisible: false
};

export {
  initial,
  showEditor,
  useEditing,
  showsMarkdownEditor,
  showsBlockEditor,
  previewIsFullWidth,
  togglePreviewWidth,
  previewIsPhone,
  toggleFrontmatter,
  toggleDirectives,
  toggleData,
  showsFrontmatterForm,
  showsDirectiveFields,
}
/* No side effect */
