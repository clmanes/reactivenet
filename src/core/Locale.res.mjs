// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let all = [
  "En",
  "Fr",
  "De",
  "Es",
  "It",
  "Pt",
  "Zh"
];

function toTag(locale) {
  switch (locale) {
    case "En" :
      return "en";
    case "Fr" :
      return "fr";
    case "De" :
      return "de";
    case "Es" :
      return "es";
    case "Pt" :
      return "pt";
    case "Zh" :
      return "zh";
    case "It" :
      return "it";
  }
}

function nativeName(locale) {
  switch (locale) {
    case "En" :
      return "English";
    case "Fr" :
      return "Français";
    case "De" :
      return "Deutsch";
    case "Es" :
      return "Español";
    case "Pt" :
      return "Português";
    case "Zh" :
      return "中文";
    case "It" :
      return "Italiano";
  }
}

function parse(tag) {
  let match = tag.trim().toLowerCase().split("-").at(0);
  if (match === undefined) {
    return;
  }
  switch (match) {
    case "de" :
      return "De";
    case "en" :
      return "En";
    case "es" :
      return "Es";
    case "fr" :
      return "Fr";
    case "it" :
      return "It";
    case "pt" :
      return "Pt";
    case "zh" :
      return "Zh";
    default:
      return;
  }
}

function fromPreferred(tags) {
  return Stdlib_Option.getOr(Stdlib_Array.reduce(tags, undefined, (found, tag) => {
    if (found !== undefined) {
      return found;
    } else {
      return parse(tag);
    }
  }), "En");
}

let fallback = "En";

export {
  fallback,
  all,
  toTag,
  nativeName,
  parse,
  fromPreferred,
}
/* No side effect */
