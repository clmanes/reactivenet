// Generated by ReScript, PLEASE EDIT WITH CARE


let marker = "#";

function parse(raw) {
  let trimmed = raw.trim();
  if (!trimmed.startsWith(marker)) {
    if (trimmed === "") {
      return;
    } else {
      return {
        TAG: "StoreKey",
        _0: trimmed
      };
    }
  }
  let key = trimmed.slice(marker.length, trimmed.length).trim();
  if (key === "") {
    return;
  } else {
    return {
      TAG: "Reactive",
      _0: key
    };
  }
}

function key(reference) {
  return reference._0;
}

function isReactive(reference) {
  return reference.TAG === "Reactive";
}

function toString(reference) {
  if (reference.TAG === "Reactive") {
    return marker + reference._0;
  } else {
    return reference._0;
  }
}

function reactiveKey(raw) {
  let match = parse(raw);
  if (match !== undefined && match.TAG === "Reactive") {
    return match._0;
  }
}

export {
  parse,
  key,
  isReactive,
  toString,
  reactiveKey,
}
/* No side effect */
