// Generated by ReScript, PLEASE EDIT WITH CARE


let segment = "j";

let path = "/" + segment;

let keyMarker = "k";

let inviteIdPattern = new RegExp("^[A-Za-z0-9_-]{8,40}$");

function isInviteId(candidate) {
  return inviteIdPattern.test(candidate);
}

function of_(origin, id, key) {
  return origin + path + "/" + id + "#" + keyMarker + key;
}

function keyOf(fragment) {
  let value = fragment.startsWith("#") ? fragment.slice(1) : fragment;
  if (value.startsWith(keyMarker) && value.length > keyMarker.length) {
    return value.slice(keyMarker.length);
  }
}

export {
  segment,
  path,
  keyMarker,
  isInviteId,
  of_,
  keyOf,
}
/* inviteIdPattern Not a pure module */
