// Generated by ReScript, PLEASE EDIT WITH CARE


function make(stamp, taken) {
  let base = stamp.replaceAll(new RegExp("[^A-Za-z0-9]", "g"), "");
  let _suffix = 0;
  while (true) {
    let suffix = _suffix;
    let candidate = suffix === 0 ? base : base + "-" + suffix.toString();
    if (!taken.includes(candidate)) {
      return candidate;
    }
    _suffix = suffix + 1 | 0;
    continue;
  };
}

export {
  make,
}
/* No side effect */
