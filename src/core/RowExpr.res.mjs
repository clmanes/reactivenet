// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Expr from "./Expr.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let withRefs = (function (source) {
  return source.replace(/[A-Za-z_][A-Za-z0-9_-]*/g, (name) => "#" + name);
});

let looksLike = (function (candidate) {
  return /[+*\/()]|\s/.test(candidate.trim()) && /[+*\/()\s]/.test(candidate);
});

function evaluate(source, lookup) {
  return Expr.evaluate(withRefs(source), lookup);
}

function parses(source) {
  return Stdlib_Option.isSome(evaluate(source, param => "1"));
}

function references(source) {
  return Expr.references(withRefs(source));
}

export {
  looksLike,
  parses,
  evaluate,
  references,
}
/* Expr Not a pure module */
