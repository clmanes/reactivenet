// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Buntest from "bun:test";
import * as KeyBundle from "./KeyBundle.res.mjs";

Buntest.describe("KeyBundle", () => {
  let bundle = {
    pub: "{\"kty\":\"EC\"}",
    salt: "c2FsdA",
    iterations: 600000,
    vault: "sealed-by-password",
    recoveryVault: "sealed-by-code"
  };
  Buntest.test("round-trips through encode and decode", () => {
    Buntest.expect(KeyBundle.decode(KeyBundle.encode(bundle))).toEqual(bundle);
  });
  Buntest.test("refuses what is not a bundle", () => {
    Buntest.expect(KeyBundle.decode("")).toEqual(undefined);
    Buntest.expect(KeyBundle.decode("{}")).toEqual(undefined);
    Buntest.expect(KeyBundle.decode("not json")).toEqual(undefined);
    Buntest.expect(KeyBundle.decode("{\"v\":2,\"pub\":\"x\"}")).toEqual(undefined);
  });
  Buntest.test("refuses a bundle with an empty seal", () => {
    let hollow = KeyBundle.encode({
      pub: "{\"kty\":\"EC\"}",
      salt: "c2FsdA",
      iterations: 600000,
      vault: "",
      recoveryVault: "sealed-by-code"
    });
    Buntest.expect(KeyBundle.decode(hollow)).toEqual(undefined);
  });
});

/*  Not a pure module */
