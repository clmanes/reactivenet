// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Buntest from "bun:test";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Translations from "./Translations.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as DirectiveClass from "./DirectiveClass.res.mjs";
import * as DirectiveRegistry from "./DirectiveRegistry.res.mjs";

Buntest.describe("DirectiveClass", () => {
  Buntest.test("every directive in the registry has a class", () => {
    let unfiled = DirectiveRegistry.all.filter(component => Stdlib_Option.isNone(DirectiveClass.of_(component.directive))).map(component => component.directive);
    Buntest.expect(unfiled.join(", ")).toBe("");
  });
  Buntest.test("no directive is filed twice", () => {
    let counted = Stdlib_Array.reduce(DirectiveClass.members, [], (all, param) => all.concat(param[1]));
    let duplicated = counted.filter(name => counted.filter(other => other === name).length > 1);
    Buntest.expect(duplicated.join(", ")).toBe("");
  });
  Buntest.test("each class has a heading of its own", () => {
    let labels = DirectiveClass.all.map($$class => Translations.translate("En", DirectiveClass.label($$class)));
    Buntest.expect(labels.length).toBe(12);
    let unique = Stdlib_Array.reduce(labels, [], (all, one) => {
      if (all.includes(one)) {
        return all;
      } else {
        return all.concat([one]);
      }
    });
    Buntest.expect(unique.length).toBe(labels.length);
  });
  Buntest.test("a directive is named the way it is written", () => {
    Buntest.expect(DirectiveClass.written("accordion-item")).toBe("::accordion-item");
    Buntest.expect(DirectiveClass.written("board")).toBe("::board");
    Buntest.expect(DirectiveClass.written("value")).toBe(":value");
    Buntest.expect(DirectiveClass.written("sum")).toBe(":sum");
  });
});

/*  Not a pure module */
