// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Buntest from "bun:test";
import * as CollectionKey from "./CollectionKey.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

Buntest.describe("CollectionKey", () => {
  Buntest.test("namespaces a collection by its app", () => {
    Buntest.expect(CollectionKey.of_("grade-book", "grades")).toBe("app/grade-book/grades");
  });
  Buntest.test("reads the path back", () => {
    let key = CollectionKey.of_("grade-book", "grades");
    Buntest.expect(Stdlib_Option.getOr(CollectionKey.pathOf("grade-book", key), "none")).toBe("grades");
  });
  Buntest.test("an app does not match another whose id merely starts the same", () => {
    let key = CollectionKey.of_("grade-book", "grades");
    Buntest.expect(Stdlib_Option.isNone(CollectionKey.pathOf("grade", key))).toBe(true);
  });
  Buntest.test("rejects keys of a different app", () => {
    let key = CollectionKey.of_("other", "grades");
    Buntest.expect(Stdlib_Option.isNone(CollectionKey.pathOf("grade-book", key))).toBe(true);
  });
  Buntest.test("rejects keys that are not collections at all", () => {
    Buntest.expect(Stdlib_Option.isNone(CollectionKey.pathOf("grade-book", "theme"))).toBe(true);
    Buntest.expect(CollectionKey.isCollection("theme")).toBe(false);
    Buntest.expect(CollectionKey.isCollection("locale")).toBe(false);
  });
  Buntest.test("recognises its own keys", () => {
    Buntest.expect(CollectionKey.isCollection(CollectionKey.of_("a", "b"))).toBe(true);
  });
  Buntest.test("a path with slashes survives", () => {
    let key = CollectionKey.of_("a", "nested/path");
    Buntest.expect(Stdlib_Option.getOr(CollectionKey.pathOf("a", key), "none")).toBe("nested/path");
  });
});

/*  Not a pure module */
