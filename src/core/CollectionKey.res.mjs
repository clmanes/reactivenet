// Generated by ReScript, PLEASE EDIT WITH CARE


let separator = "/";

let prefix = "app" + separator;

function of_(app, path) {
  return prefix + app + separator + path;
}

function appPrefix(app) {
  return prefix + app + separator;
}

function pathOf(app, key) {
  let start = appPrefix(app);
  if (!key.startsWith(start)) {
    return;
  }
  let path = key.slice(start.length, key.length);
  if (path === "") {
    return;
  } else {
    return path;
  }
}

function isCollection(key) {
  return key.startsWith(prefix);
}

export {
  of_,
  appPrefix,
  pathOf,
  isCollection,
}
/* No side effect */
