// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Collection from "./Collection.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let created = "createdAt";

let updated = "updatedAt";

let reserved = [
  created,
  updated
];

function isReserved(name) {
  return reserved.some(known => known.toLowerCase() === name.toLowerCase());
}

function without(fields) {
  return fields.filter(field => !isReserved(field.name));
}

function apply(fields, now, createdAt) {
  let user = without(fields);
  let creation_value = Stdlib_Option.getOr(createdAt, now);
  let creation = {
    name: created,
    value: creation_value
  };
  let modification = {
    name: updated,
    value: now
  };
  return user.concat([
    creation,
    modification
  ]);
}

function createdOf(record) {
  return Collection.field(record, created);
}

export {
  created,
  updated,
  reserved,
  isReserved,
  without,
  apply,
  createdOf,
}
/* No side effect */
