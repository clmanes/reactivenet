// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Buntest from "bun:test";
import * as SourceSeed from "./SourceSeed.res.mjs";

Buntest.describe("SourceSeed.decide", () => {
  Buntest.test("nothing stored yet takes what the document says", () => {
    Buntest.expect(SourceSeed.decide(undefined, "50", undefined)).toEqual({
      TAG: "Adopt",
      _0: "50"
    });
  });
  Buntest.test("an unchanged document leaves the live value alone", () => {
    Buntest.expect(SourceSeed.decide("80", "50", "50")).toEqual({
      TAG: "Keep",
      _0: "80"
    });
  });
  Buntest.test("a document that changed its mind wins", () => {
    Buntest.expect(SourceSeed.decide("80", "60", "50")).toEqual({
      TAG: "Adopt",
      _0: "60"
    });
  });
  Buntest.test("a first bind of a key that is already stored adopts, having nothing to compare", () => {
    Buntest.expect(SourceSeed.decide("80", "50", undefined)).toEqual({
      TAG: "Adopt",
      _0: "50"
    });
  });
});

Buntest.describe("SourceSeed ticks", () => {
  Buntest.test("round-trips through the string a collection stores", () => {
    Buntest.expect(SourceSeed.ofTick(true)).toBe("true");
    Buntest.expect(SourceSeed.ofTick(false)).toBe("false");
    Buntest.expect(SourceSeed.ticked("true")).toBe(true);
    Buntest.expect(SourceSeed.ticked("false")).toBe(false);
  });
  Buntest.test("anything else is not ticked", () => {
    Buntest.expect(SourceSeed.ticked("")).toBe(false);
    Buntest.expect(SourceSeed.ticked("on")).toBe(false);
    Buntest.expect(SourceSeed.ticked("TRUE")).toBe(false);
  });
});

/*  Not a pure module */
