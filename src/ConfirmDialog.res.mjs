// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Spectrum from "./spectrum/Spectrum.res.mjs";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as JsxRuntime from "react/jsx-runtime";

function ConfirmDialog(props) {
  let onCancel = props.onCancel;
  let onConfirm = props.onConfirm;
  let open_ = props.open_;
  let dialog = React.useRef(null);
  let titleId = "rn-confirm-title";
  React.useEffect(() => {
    let match = dialog.current;
    if (match == null) {
      return;
    }
    if (!open_) {
      return;
    }
    let dismissed = () => onCancel();
    match.addEventListener("close", dismissed);
    if (!match.open) {
      match.showModal();
    }
    return () => {
      match.removeEventListener("close", dismissed);
      if (match.open) {
        match.close();
        return;
      }
    };
  }, [open_]);
  return JsxRuntime.jsxs("dialog", {
    children: [
      JsxRuntime.jsx("h2", {
        children: props.title,
        className: "rn-dialog-title",
        id: titleId
      }),
      JsxRuntime.jsx("p", {
        children: props.message,
        className: "rn-dialog-message"
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx(Spectrum.Button.make, {
            variant: "secondary",
            treatment: "outline",
            onClick: param => onCancel(),
            children: props.cancelLabel
          }),
          JsxRuntime.jsx(Spectrum.Button.make, {
            variant: "negative",
            onClick: param => onConfirm(),
            children: props.confirmLabel
          })
        ],
        className: "rn-dialog-actions"
      })
    ],
    ref: Primitive_option.some(dialog),
    "aria-labelledby": titleId,
    className: "rn-dialog"
  });
}

let make = ConfirmDialog;

export {
  make,
}
/* react Not a pure module */
