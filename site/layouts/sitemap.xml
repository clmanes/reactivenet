{{/* La sitemap di Hugo, con una sola differenza: le pagine marcate noindex
     non ci sono. Una pagina esclusa dall'indice ma dichiarata nella sitemap è
     un segnale che contraddice l'altro, e i motori lo trattano come un
     errore da segnalare. Il criterio è lo stesso di head.html — la sezione
     note-legali, più il parametro `noindex` per i casi singoli. */}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Data.Pages }}
  {{- if and .Permalink (not (or .Params.noindex (eq .Section "note-legali"))) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML (.Lastmod.Format "2006-01-02T15:04:05-07:00") }}</lastmod>
    {{- end }}
    {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}"/>
    {{- end }}
  </url>
  {{- end }}
  {{ end }}
</urlset>
